/**
 * Package version, mirrored from `package.json` at publish time.
 *
 * Placeholder public export for Stage 0 scaffolding — real primitives,
 * transforms, and STL export are added in later stages.
 */
export const VERSION = '0.0.0';
